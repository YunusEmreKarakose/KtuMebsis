html(lang='en')
  head
    meta(charset='utf-8')
    meta(name='viewport', content='width=device-width, initial-scale=1, shrink-to-fit=no')
    meta(name='description', content='')
    meta(name='author', content='')
    title GET
    // Bootstrap core CSS
    link(href='vendor/bootstrap/css/bootstrap.min.css', rel='stylesheet')
    // Custom styles for this template
    link(href='css/simple-sidebar.css', rel='stylesheet')
  body
    #wrapper.d-flex
      // Sidebar
      #sidebar-wrapper.bg-light.border-right
        .sidebar-heading KTU MEBSİS 
        .list-group.list-group-flush        
          form(action='/cmp', method='get')
            button.list-group-item.list-group-item-action.bg-light(type='submit') Firma Bilgisi Oluştur
          form(action='/ex', method='get')
            button.list-group-item.list-group-item-action.bg-light(type='submit') Eski Çalışma Bilgisi Kaydet
          form(action='/pst', method='get')
            button.list-group-item.list-group-item-action.bg-light(type='submit') Paylaşım Yap
          form(action='/yrm', method='get')
            button.list-group-item.list-group-item-action.bg-light(type='submit') Yorum Yap
          form(action='/msg', method='get')
            button.list-group-item.list-group-item-action.bg-light(type='submit') Mesaj At
          form(action='/gt', method='get')
            button.list-group-item.list-group-item-action.bg-light(type='submit') Sorgulamalar
          form(action='/prf', method='get')
            button.list-group-item.list-group-item-action.bg-light(type='submit') Profil
      // /#sidebar-wrapper
      // Page Content
      #page-content-wrapper
        nav.navbar.navbar-expand-lg.navbar-light.bg-light.border-bottom
          button#menu-toggle.btn.btn-primary Menu Gizle
          button.navbar-toggler(type='button', data-toggle='collapse', data-target='#navbarSupportedContent', aria-controls='navbarSupportedContent', aria-expanded='false', aria-label='Toggle navigation')
            span.navbar-toggler-icon
          #navbarSupportedContent.collapse.navbar-collapse
            ul.navbar-nav.ml-auto.mt-2.mt-lg-0
              li.nav-item.active
                form(action='/mp', method='get')
                  button(type='submit') ANASAYFA
              li.nav-item
                a.nav-link(href='#') Link
              li.nav-item.dropdown
                a#navbarDropdown.nav-link.dropdown-toggle(href='#', role='button', data-toggle='dropdown', aria-haspopup='true', aria-expanded='false')
                  | Menü
                .dropdown-menu.dropdown-menu-right(aria-labelledby='navbarDropdown')
                  form(action='/mp', method='get')
                    button.dropdown-item(type='submit') Anasayfa
                  form(action='/anket', method='get')
                    button.dropdown-item(type='submit') Mezun Anket
                  .dropdown-divider
                  form(action='/quit', method='get')
                    button.dropdown-item(type='submit') Çıkış
        .container-fluid
            .form
                .tab-content
                    #signup
                    h1 Sorgular
                    form(action='/get/get1', method='get')
                        button.button.button-block(type='submit') En çok mezunun çalıştığı şirket
                    p1 Iş ilanlarını çalışma alanına yada firmaya göre filtreleme
                    form(action='/get/get2', method='post')
                        .top-row
                        .field-wrap
                            label
                            | Tip
                            input(type='text', autocomplete='off',name='type')
                        .field-wrap
                            label
                            | Şirket ismi 
                            input(type='text', autocomplete='off',name='name')
                        button.button.button-block(type='submit') fitrele 
                    p2 STAJ ilanlarını çalışma dönemi , firmaya veya süreye göre filtreleme
                    form(action='/get/get3', method='post')
                        .top-row
                        .field-wrap
                            label
                            | staj dönemi
                            input(type='text', autocomplete='off',name='period')
                        .field-wrap
                            label
                            | Şirket ismi 
                            input(type='text', autocomplete='off',name='name')
                        .field-wrap
                            label
                            | Süre 
                            input(type='text', autocomplete='off',name='duration')
                        button.button.button-block(type='submit') fitrele 
                    p3 Kayıtlı Mezun sayısı
                    form(action='/get/get4', method='get')
                        button.button.button-block(type='submit') Kayıtlı Mezun sayısı
                    p4 Kayıtlı Şirket sayısı
                    form(action='/get/get5', method='get')
                        button.button.button-block(type='submit') Kayıtlı Şirket sayısı
                    p5 Hangi Şirkette Ne Kadar Mezun Var
                    form(action='/get/get6', method='get')
                        button.button.button-block(type='submit') Sorgula
                    p6 Öğrenci no ile anket dolduruldumu sorgusu
                    form(action='/get/get7', method='post')
                        .field-wrap
                            label
                            | Öğrenci No 
                            input(type='text', autocomplete='off',name='stuNum')
                        button.button.button-block(type='submit') Sorgula 
                // tab-content
                // /form
      // /#page-content-wrapper
      // /#wrapper
      // Bootstrap core JavaScript
      script(src='vendor/jquery/jquery.min.js')
      script(src='vendor/bootstrap/js/bootstrap.bundle.min.js')
      // Menu Toggle Script
      script.
        $("#menu-toggle").click(function(e) {
        e.preventDefault();
        $("#wrapper").toggleClass("toggled");
        });
